package XmlParsing.rules

import scala.collection.mutable

object RulesParser {
   def parse(path:String):Seq[FunctionRule] = {
      val rules = mutable.ArrayBuffer[FunctionRule]()
      rules
   }
}
